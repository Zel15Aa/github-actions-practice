name: my-hard-work

on:
  push:
    branches: [main]

jobs:
  practise-job:
    runs-on: ubuntu-latest

    steps:
      - name: download code
        uses: actions/checkout@v4

      - name: displays the time and date
        run: date

      - name: display list of files and folders
        run: ls -la
